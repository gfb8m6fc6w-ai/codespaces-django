# 🎯 نظام البلياردو المتقدم 5A Diamond System Pro

## نظام احترافي متكامل لتحليل تسديدات البلياردو

[![Python Version](https://img.shields.io/badge/python-3.9+-blue.svg)](https://www.python.org/downloads/)
[![Django](https://img.shields.io/badge/django-5.2+-green.svg)](https://www.djangoproject.com/)
[![FastAPI](https://img.shields.io/badge/fastapi-0.104+-critical.svg)](https://fastapi.tiangolo.com/)

---

## 📋 المحتويات

- [✨ الميزات](#-الميزات)
- [🚀 البدء السريع](#-البدء-السريع)
- [📁 البنية](#-البنية)
- [🔌 الواجهات البرمجية](#-الواجهات-البرمجية)
- [🧪 الاختبار](#-الاختبار)
- [📝 التطوير](#-التطوير)

---

## ✨ الميزات

### 🔬 محرك حساب متقدم

- ✅ حساب دقيق لمعاملات التسديقة
- ✅ نظام تصنيف الصعوبة (5 مستويات)
- ✅ حساب معدل النجاح المتوقع
- ✅ حساب القوة والزاوية المطلوبة
- ✅ نظام الجدران العشري (10-Diamond System)

### 📊 إحصائيات شاملة

- ✅ تتبع كامل للتسديقات
- ✅ إحصائيات حسب عدد الجدران
- ✅ إحصائيات حسب مستوى الصعوبة
- ✅ معدل النجاح الكلي ومتوسط الصعوبة
- ✅ تصدير واستيراد البيانات

### 🌐 واجهات متعددة

- ✅ **REST API** (FastAPI)
- ✅ **Web Dashboard** (HTML/CSS/JS)
- ✅ **Django Admin**
- ✅ **Fallback HTTP Server** (بدون FastAPI)

### 💾 تخزين ذكي

- ✅ حفظ محلي (JSON)
- ✅ استيراد وتصدير البيانات
- ✅ نسخ احتياطية تلقائية
- ✅ دعم SQLite للبيانات المعقدة

---

## 🚀 البدء السريع

### المتطلبات

- Python 3.9+
- pip

### 1️⃣ التثبيت

```bash
# استنساخ المشروع
cd /workspaces/codespaces-django

# تثبيت المكتبات
pip install -r requirements.txt

# إنشاء المجلدات المطلوبة
mkdir -p .billiards_data logs
```

### 2️⃣ تشغيل الخادم

#### الخيار الأول: FastAPI (مفضّل)

```bash
python api.py
```

سيبدأ الخادم على: `http://localhost:8001`

#### الخيار الثاني: خادم بديل (بدون FastAPI)

إذا لم تكن FastAPI مثبتة:

```bash
python run_server.py
```

#### الخيار الثالث: Django

```bash
python manage.py runserver
```

سيبدأ على: `http://localhost:8000`

### 3️⃣ الوصول إلى الواجهة

- **API Documentation**: [http://localhost:8001/docs](http://localhost:8001/docs)
- **Web Dashboard**: [http://localhost:8000](http://localhost:8000)
- **Health Check**: [http://localhost:8001/health](http://localhost:8001/health)

---

## 📁 البنية

```python
codespaces-django/
│
├── backend/                          # الخلفية
│   ├── billiards/                   # محرك البلياردو
│   │   ├── __init__.py
│   │   ├── calculator.py            # حساب التسديقات (225 سطر)
│   │   ├── engine.py                # محرك البلياردو (187 سطر)
│   │   └── rail_system.py           # نظام الجدران (182 سطر)
│   │
│   └── models/                      # نماذج البيانات
│       ├── __init__.py
│       ├── shot.py                  # نموذج التسديقة (167 سطر)
│       └── statistics.py            # الإحصائيات (66 سطر)
│
├── frontend/                        # الواجهة الأمامية
│   ├── html/                       # ملفات HTML
│   │   ├── billiards-calculator.html
│   │   ├── measurements-manager.html
│   │   └── unified-app.html
│   ├── css/                        # ملفات CSS
│   │   └── main.css
│   └── js/                         # ملفات JavaScript
│       ├── billiards-engine.js
│       ├── system-services.js
│       └── integrated-shot-system.js
│
├── hello_world/                    # تطبيق Django
│   ├── settings.py
│   ├── urls.py
│   ├── wsgi.py
│   └── asgi.py
│
├── api.py                          # REST API (FastAPI)
├── run_server.py                   # خادم بديل (HTTP)
├── config_settings.py              # إعدادات المشروع
├── test_system.py                  # اختبار شامل
│
├── requirements.txt                # المكتبات المطلوبة
├── manage.py                       # Django CLI
├── GUIDE.md                        # دليل الاستخدام
└── README.md                       # هذا الملف
```

---

## 🔌 الواجهات البرمجية

### 📡 نقاط نهاية API الرئيسية

#### 1. حساب تسديقة جديدة

```bash
POST /api/v1/calculate

المعاملات:
  - rails: عدد الجدران (1-4)
  - cue_position: موضع العصا (0-10)
  - white_ball: موضع الكرة البيضاء (0-10)
  - target: موضع الهدف (0-10)
  - pocket: رقم الجيب (0-5)

مثال:
curl -X POST "http://localhost:8001/api/v1/calculate?rails=2&cue_position=5&white_ball=3&target=2&pocket=1"
```

#### 2. الحصول على التسديقات

```bash
GET /api/v1/shots

المعاملات (اختيارية):
  - rails: تصفية حسب الجدران
  - difficulty: تصفية حسب الصعوبة
  - skip: تخطي عدد من النتائج
  - limit: عدد النتائج المرجعة

مثال:
curl "http://localhost:8001/api/v1/shots?rails=2&limit=10"
```

#### 3. الحصول على الإحصائيات

```bash
GET /api/v1/statistics

مثال:
curl http://localhost:8001/api/v1/statistics
```

#### 4. الإحصائيات حسب الجدران

```bash
GET /api/v1/statistics/by-rails

مثال:
curl http://localhost:8001/api/v1/statistics/by-rails
```

#### 5. الإحصائيات حسب الصعوبة

```bash
GET /api/v1/statistics/by-difficulty

مثال:
curl http://localhost:8001/api/v1/statistics/by-difficulty
```

#### 6. فحص صحة الخادم

```bash
GET /health

مثال:
curl http://localhost:8001/health
```

---

## 🧪 الاختبار

### تشغيل الاختبار الشامل

```bash
python test_system.py
```

سيختبر:
- ✅ استيراد جميع المكتبات
- ✅ تهيئة محرك البلياردو
- ✅ حساب التسديقات
- ✅ تسجيل النتائج
- ✅ الإحصائيات
- ✅ التخزين والاسترجاع
- ✅ التصدير والاستيراد

## الاختبارات المخصصة

```bash
# اختبار البلياردو
python test_billiards.py

# اختبار النظام
pytest test_system.py -v
```

---

## 📝 التطوير

### إعدادات البيئة

قم بإنشاء ملف `.env`:

```bash
cp .env.example .env
```

ثم عدّل القيم:

```env
SECRET_KEY=your-secret-key-here
DEBUG=False
SERVER_HOST=0.0.0.0
SERVER_PORT=8001
```

### الإعدادات الرئيسية

انظر `config_settings.py` للإعدادات الكاملة:

```python
# عدد الجدران
MIN_RAILS = 1
MAX_RAILS = 4

# نطاق المواضع
MIN_POSITION = 0.0
MAX_POSITION = 10.0

# مستويات الصعوبة
DIFFICULTY_LEVELS = ["سهلة", "متوسطة", "صعبة", "جداً صعبة", "قصوى"]
```

### البنية الأساسية للتسديقة

```python
@dataclass
class Shot:
    rails: int                      # عدد الجدران
    cue_position: float            # موضع العصا
    white_ball: float              # موضع الكرة البيضاء
    target: float                  # موضع الهدف
    pocket: int                    # رقم الجيب
    difficulty: Difficulty         # مستوى الصعوبة
    success_rate: float            # احتمال النجاح
    executed: bool                 # هل تم تنفيذها
    result: Optional[ShotResult]  # النتيجة
    timestamp: datetime           # وقت الإنشاء
```

---

## 🔧 استكشاف الأخطاء

### المشكلة: FastAPI غير مثبتة

**الحل:**

```bash
pip install fastapi uvicorn
# أو استخدم الخادم البديل
python run_server.py
```

### المشكلة: خطأ في الاستيراد

**الحل:**

```bash
pip install -r requirements.txt
```

### المشكلة: خطأ في الوصول للبيانات

**الحل:**

```bash
mkdir -p .billiards_data
chmod 755 .billiards_data
```

---

## 📊 أمثلة عملية

### مثال 1: حساب تسديقة بسيطة

```bash
curl -X POST "http://localhost:8001/api/v1/calculate?rails=1&cue_position=2.5&white_ball=3.0&target=5.5&pocket=0"
```

### مثال 2: الحصول على تسديقات معقدة

```bash
curl "http://localhost:8001/api/v1/shots?rails=4&difficulty=قصوى"
```

### مثال 3: تصدير جميع البيانات

```bash
curl -X POST "http://localhost:8001/api/v1/export" | jq . > billiards_data.json
```

---

## 📈 الإحصائيات

### استجابة الإحصائيات

```json
{
  "total_calculations": 10,
  "total_shots_attempted": 8,
  "total_shots_successful": 5,
  "success_rate": 62.5,
  "average_difficulty": "متوسطة"
}
```

---

## 🛠️ الأدوات المستخدمة

- **Python 3.9+** - لغة البرمجة
- **Django 5.2.2** - إطار عمل الويب
- **FastAPI 0.104.0** - إطار عمل API
- **SQLite** - قاعدة البيانات
- **JSON** - تخزين البيانات

---

## 📄 الترخيص

جميع الحقوق محفوظة لفريق 5A System Pro

---

## 👥 فريق التطوير

- **المطورون**: فريق 5A System Pro
- **آخر تحديث**: ٢٠٢٤
- **الإصدار**: ٢.٠.٠

---

## 📞 الدعم والمساعدة

للمساعدة والدعم:

1. تحقق من [GUIDE.md](GUIDE.md) للتفاصيل الكاملة
2. اقرأ تعليقات الكود في ملفات المشروع
3. شغّل `test_system.py` للتشخيص

---

## 🎯 الخطوات التالية

- [ ] إضافة مصادقة المستخدم
- [ ] دعم قاعدة بيانات MySQL
- [ ] تطبيق الهاتف المحمول
- [ ] تحليلات متقدمة
- [ ] نظام التوصيات

---

## شكراً لاستخدام النظام

شكراً لاستخدام نظام 5A Diamond System Pro! 🎉
