"""
دليل استكشاف الأخطاء والمشاكل
===============================

هذا الملف يحتوي على حلول للمشاكل الشائعة
"""

# ==============================================================================
# 1️⃣ مشاكل الاستيراض والمكتبات
# ==============================================================================

"""
مشكلة: Import Error - FastAPI/uvicorn غير مثبت
-----------------------------------------------

الأعراض:
  ModuleNotFoundError: No module named 'fastapi'
  ModuleNotFoundError: No module named 'uvicorn'

الحل:
  1. تثبيت FastAPI و uvicorn:
     pip install fastapi uvicorn
  
  2. أو استخدام الخادم البديل:
     python run_server.py
  
  3. تثبيت جميع المكتبات المطلوبة:
     pip install -r requirements.txt

الفحص:
  python -c "import fastapi; print('FastAPI متاح')"
"""

# ==============================================================================
# 2️⃣ مشاكل مسارات البيانات
# ==============================================================================

"""
مشكلة: FileNotFoundError - لا يمكن العثور على ملفات البيانات
-----------------------------------------------------------

الأعراض:
  FileNotFoundError: .billiards_data/shots.json غير موجود
  PermissionError: لا توجد صلاحيات الوصول

الحل:
  1. إنشاء مجلد البيانات:
     mkdir -p .billiards_data
     mkdir -p logs
  
  2. إعطاء الصلاحيات:
     chmod 755 .billiards_data
  
  3. إنشاء ملفات البيانات الأولية:
     python initialize.py

الفحص:
  ls -la .billiards_data/
"""

# ==============================================================================
# 3️⃣ مشاكل الخادم والمنافذ
# ==============================================================================

"""
مشكلة: OSError - المنفذ 8001 قيد الاستخدام
------------------------------------------

الأعراض:
  Address already in use
  [Errno 48] port is in use

الحل:
  1. معرفة أي عملية تستخدم المنفذ:
     lsof -i :8001
  
  2. إيقاف العملية:
     kill -9 <PID>
  
  3. استخدام منفذ مختلف:
     SERVER_PORT=8002 python api.py

الفحص:
  netstat -tulpn | grep 8001
"""

# ==============================================================================
# 4️⃣ مشاكل قاعدة البيانات
# ==============================================================================

"""
مشكلة: SQLite - خطأ في الوصول للقاعدة
--------------------------------------

الأعراض:
  sqlite3.OperationalError: database is locked
  sqlite3.DatabaseError: file is not a database

الحل:
  1. حذف ملف قاعدة البيانات الفاسد:
     rm .billiards_data/billiards.db
  
  2. إعادة إنشاء البيانات:
     python initialize.py
  
  3. التحقق من الصلاحيات:
     chmod 644 .billiards_data/billiards.db

الفحص:
  python -c "import sqlite3; sqlite3.connect('.billiards_data/billiards.db')"
"""

# ==============================================================================
# 5️⃣ مشاكل استيراد المشروع
# ==============================================================================

"""
مشكلة: Import Error - لا يمكن استيراد مكتبات المشروع
--------------------------------------------------

الأعراض:
  ImportError: No module named 'backend'
  ModuleNotFoundError: No module named 'backend.billiards.engine'

الحل:
  1. التأكد من تشغيل الأمر من مجلد المشروع:
     cd /workspaces/codespaces-django
  
  2. إضافة مسار المشروع:
     export PYTHONPATH=$PYTHONPATH:/workspaces/codespaces-django
  
  3. التحقق من وجود ملفات __init__.py:
     ls backend/__init__.py
     ls backend/billiards/__init__.py
     ls backend/models/__init__.py

الفحص:
  python -c "from backend.billiards.engine import BilliardsEngine; print('OK')"
"""

# ==============================================================================
# 6️⃣ مشاكل الاختبارات
# ==============================================================================

"""
مشكلة: فشل الاختبارات
---------------------

الأعراض:
  FAILED test_system.py
  AssertionError: ...
  الاختبار يتعطل

الحل:
  1. تشغيل الاختبارات بوضوح:
     python test_system.py -v
  
  2. الاختبار الفردي:
     python test_billiards.py
  
  3. مسح البيانات القديمة وإعادة المحاولة:
     rm -rf .billiards_data/*
     python initialize.py
     python test_system.py

الفحص:
  python -m pytest test_system.py -v
"""

# ==============================================================================
# 7️⃣ مشاكل الأداء
# ==============================================================================

"""
مشكلة: الخادم بطيء جداً
------------------------

الأعراض:
  - استجابة الطلبات تأخذ وقتاً طويلاً
  - استهلاك عالي للذاكرة

الحل:
  1. تقليل حجم سجلات البيانات:
     rm .billiards_data/shots.json
  
  2. استخدام DEBUG=False في الإنتاج:
     DEBUG=False python api.py
  
  3. زيادة موارد النظام:
     - RAM: الحد الأدنى 512MB
     - CPU: معالج ثنائي النوى على الأقل

الفحص:
  top -p $(pgrep -f "python api.py")
"""

# ==============================================================================
# 8️⃣ مشاكل التوافقية
# ==============================================================================

"""
مشكلة: عدم التوافق مع إصدار Python
----------------------------------

الأعراض:
  - أخطاء في بناء الجملة
  - AttributeError في مكتبات
  - عدم دعم f-strings

الحل:
  1. التحقق من إصدار Python:
     python --version
  
  2. يجب استخدام Python 3.9+:
     python3.9 api.py
     python3.10 api.py
     python3.11 api.py
  
  3. تحديث Python:
     apt-get install python3.11
     update-alternatives --install /usr/bin/python python /usr/bin/python3.11 1

الفحص:
  python -c "import sys; print(sys.version_info)"
"""

# ==============================================================================
# 9️⃣ مشاكل متغيرات البيئة
# ==============================================================================

"""
مشكلة: متغيرات البيئة غير محملة
-------------------------------

الأعراض:
  - DEBUG=True دائماً
  - SECRET_KEY فارغة
  - PORT غير صحيح

الحل:
  1. إنشاء ملف .env:
     cp .env.example .env
  
  2. تحرير ملف .env:
     nano .env
     # عدّل القيم حسب الحاجة
  
  3. تحميل متغيرات البيئة:
     export DEBUG=False
     export SECRET_KEY=your-secret-key
     python api.py

الفحص:
  cat .env
  echo $DEBUG
"""

# ==============================================================================
# 🔟 مشاكل الأمان
# ==============================================================================

"""
مشكلة: تحذيرات الأمان
-------------------

الأعراض:
  - تحذير: DEBUG=True
  - تحذير: SECRET_KEY غير محدد
  - تحذير: CORS السماح للجميع

الحل:
  1. تعطيل DEBUG في الإنتاج:
     DEBUG=False python api.py
  
  2. تعيين SECRET_KEY آمن:
     export SECRET_KEY=$(python -c 'import secrets; print(secrets.token_urlsafe(32))')
  
  3. تحديد قائمة CORS المسموحة:
     # في api.py، عدّل:
     allow_origins=["http://localhost:3000", "http://localhost:8000"]

الفحص:
  curl -I http://localhost:8001/
  # تحقق من رؤوس الأمان
"""

# ==============================================================================
# 🔧 الأدوات المساعدة
# ==============================================================================

"""
أدوات التشخيص المفيدة:

1. فحص صحة النظام:
   python test_system.py

2. عرض الإعدادات:
   python config_settings.py

3. معرفة تفاصيل الخطأ:
   python -c "import traceback; ..."

4. مراقبة الحالة الحية:
   tail -f billiards.log

5. اختبار API يدوياً:
   curl http://localhost:8001/health

6. تصدير البيانات للتحليل:
   curl http://localhost:8001/api/v1/export > data.json
"""

# ==============================================================================
# 📞 الدعم والمساعدة
# ==============================================================================

"""
إذا استمرت المشكلة:

1. اقرأ السجل الكامل:
   cat billiards.log | tail -50

2. تشغيل بوضع التصحيح:
   DEBUG=True python api.py

3. تشغيل الاختبارات:
   python test_system.py

4. حذف جميع البيانات والبدء من جديد:
   rm -rf .billiards_data/*
   python initialize.py
   python test_system.py

5. إعادة تثبيت المكتبات:
   pip install --upgrade -r requirements.txt

6. التحقق من التوثيق:
   cat GUIDE.md
   cat README_FULL.md
"""

if __name__ == "__main__":
    print(__doc__)
