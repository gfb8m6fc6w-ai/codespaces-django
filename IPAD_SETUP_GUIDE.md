# 📱 شرح مفصل: تشغيل المشروع على iPad

## 🎯 المشكلة التي واجهتك وحلها

المشكلة الأساسية كانت أن **الخادم كان يستمع فقط على localhost**، ولا يمكن الوصول إليه من أجهزة خارجية (مثل iPad).

### ✅ الحلول التي تم تطبيقها:

1. **تفعيل ALLOWED_HOSTS للجميع** (`*`)
2. **إضافة Codespaces CSRF origins**
3. **تعطيل فحوصات SSL للتطوير**
4. **إضافة endpoints صحة للاختبار**

---

## 🚀 خطوات التشغيل الكاملة

### **الخطوة 1: فتح Codespaces**

```
1. اذهب إلى: https://github.com/codespaces
2. اضغط على "codespaces-django"
3. انتظر فتح البيئة (10-15 ثانية)
```

### **الخطوة 2: تشغيل الخادم**

في Terminal داخل Codespaces، اكتب:

```bash
python manage.py runserver 0.0.0.0:8000
```

**ستظهر رسالة مشابهة لهذه:**

```
Watching for file changes with StatReloader
Quit the server with CONTROL-C.

Starting development server at http://127.0.0.1:8000/
```

### **الخطوة 3: نسخ الرابط**

عندما يعمل الخادم، ستظهر نافذة صغيرة في الزاوية السفلى اليمنى:

```
Port 8000 is available. Open in browser?
```

**اضغط على "Open in browser"** 

سيفتح الخادم في نفس النافذة. انسخ الرابط من شريط العنوان.

### **الخطوة 4: الدخول من iPad**

#### **الطريقة الأولى - المباشرة:**

1. افتح Safari على iPad
2. الصق الرابط الذي نسخته من Codespaces
3. اضغط "Go"

#### **الطريقة الثانية - اليدوية:**

إذا لم تعرف الرابط الدقيق:

```
1. افتح Codespaces على الويب
2. ابحث عن الرابط في شريط العنوان
3. يجب أن يكون شيء مثل:
   https://super-duper-garbanzo-xxx-8000.preview.app.github.dev/
4. انسخه على iPad
```

---

## 🔧 الاختبارات التشخيصية

### **اختبار 1: التحقق من الخادم يعمل**

```bash
curl http://localhost:8000/health/
```

**النتيجة المتوقعة:**
```json
{
    "status": "ok",
    "message": "الخادم يعمل بنجاح ✅",
    "debug": true
}
```

### **اختبار 2: التحقق من الصفحة الرئيسية**

```bash
curl -I http://localhost:8000/
```

**النتيجة المتوقعة:**
```
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8
```

---

## 🛠️ حل المشاكل الشائعة

### ❌ **المشكلة: "لا يمكن الوصول إلى الخادم من iPad"**

**الأسباب المحتملة:**

| المشكلة | الحل |
|--------|------|
| الخادم معطل | شغله بـ: `python manage.py runserver 0.0.0.0:8000` |
| الـ WiFi مقطوع | تأكد من اتصال iPad بنفس الشبكة |
| الرابط خاطئ | انسخ الرابط من شريط العنوان في Codespaces |
| Firewall يحجب | جرب الرابط على نفس الجهاز أولاً |

### ❌ **المشكلة: "خطأ CORS"**

**الحل:**
```python
# تم تفعيل CORS للجميع في settings.py
CSRF_TRUSTED_ORIGINS = ['http://localhost:8000', 'http://127.0.0.1:8000', 'https://*']
```

### ❌ **المشكلة: "الصفحة بيضاء"**

**جرب:**
```bash
python manage.py collectstatic --noinput
```

---

## 📊 الإعدادات الحالية

### **settings.py:**
- ✅ `DEBUG = True`
- ✅ `ALLOWED_HOSTS = ['*']`
- ✅ `CSRF_TRUSTED_ORIGINS` محدثة
- ✅ `X_FRAME_OPTIONS = "ALLOW-FROM PREVIEW.APP.GITHUB.DEV"`
- ✅ `SESSION_COOKIE_SECURE = False` (للتطوير)

### **urls.py:**
- ✅ `/` - الصفحة الرئيسية
- ✅ `/health/` - فحص صحة الخادم
- ✅ `/api/health/` - API للفحص
- ✅ `/admin/` - لوحة التحكم

---

## 🎬 مثال عملي كامل

### **على جهاز الحاسوب (Codespaces):**

```bash
# 1. التأكد من المكتبات
pip install -r requirements.txt

# 2. الهجرات
python manage.py migrate

# 3. الملفات الثابتة
python manage.py collectstatic --noinput

# 4. تشغيل الخادم
python manage.py runserver 0.0.0.0:8000
```

### **على iPad:**

```
1. افتح Safari
2. اكتب الرابط الذي حصلت عليه من Codespaces
3. سترى الصفحة الرئيسية ✅
```

---

## 📱 نصائح iPad

### **حفظ الرابط كإشارة مرجعية:**

1. في Safari، اضغط على أيقونة المشاركة
2. اختر "Add Bookmark"
3. ضعه في مجلد يسهل الوصول إليه

### **فتح في نافذة منفصلة:**

1. اضغط وأمسك على الرابط
2. اختر "Open in Split View"

### **إعادة التحميل بسهولة:**

```
اسحب من أعلى الشاشة لأسفل = تحديث الصفحة
```

---

## 🔍 معلومات مهمة

### **الرابط الصحيح:**

```
✅ https://codespaces-name-8000.preview.app.github.dev/
❌ http://localhost:8000 (فقط من نفس الجهاز)
❌ http://0.0.0.0:8000 (لا يعمل من الخارج)
```

### **المنافذ:**

- **8000** = Django development server
- **5000** = للـ API إذا أردت
- **3306** = Database (MySQL) - لم يتم تفعيله

---

## 💡 نصائح إضافية

### **مراقبة السجلات:**

```bash
# عرض السجلات الأخيرة
tail -f /workspaces/codespaces-django/logs/app.log

# عرض حركة الشبكة
lsof -i :8000
```

### **إعادة التشغيل بسرعة:**

```bash
# إيقاف الخادم
Ctrl + C

# إعادة التشغيل
python manage.py runserver 0.0.0.0:8000
```

### **اختبار من محطة طرفية مختلفة:**

```bash
# اختبر من terminal جديد بينما الخادم يعمل
curl -s http://localhost:8000/health/ | python -m json.tool
```

---

## ✅ قائمة التحقق

قبل الدخول من iPad، تأكد من:

- [ ] الخادم يعمل (ستشاهد رسالة "Starting development server")
- [ ] لا توجد أخطاء في Codespaces Terminal
- [ ] iPad متصل بـ WiFi
- [ ] نسخت الرابط بشكل صحيح
- [ ] افتحت الرابط في Safari

---

## 🆘 تواصل مع الدعم

إذا لم ينجح شيء:

1. أعد تشغيل Codespaces
2. نظف ذاكرة التخزين المؤقت على iPad (Settings > Safari > Clear History)
3. حاول من متصفح آخر على iPad (Chrome/Edge)
4. تأكد من أن الرابط بدون أخطاء إملائية

---

**تم تحديثه:** 2 يناير 2026  
**الإصدار:** 2.0  
**الحالة:** ✅ جاهز للاستخدام
